<!DOCTYPE html>
<html>
  
  <head>
    <title><%= oMediaClients[sRoom].sName %> - Media Menu</title>
    <link rel="stylesheet" type="text/css" href="/static/style.css" />
    <style type="text/css" rel="stylesheet">
      body{overflow:hidden;}
      .button{margin-left:0;}
      #header{height:80px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;position:relative;z-index:600;display:block;background:rgba(0,0,0,0.8);font-size:2em;border-bottom:5px solid #fff;color:#fff;text-align:center;font-weight:bold;line-height:80px;margin-bottom:10px;}
      #opener{position:absolute;left:0;top:0;width:100%;height:100%;z-index:500;margin:0;padding:0;}
      #opener video{width:100%;height:100%;}
      #gate_switch{position:absolute;width:94%;left:3%;bottom:3%;text-indent:0;font-size:2em;}
    </style>
    <meta name="viewport" content="initial-scale=1,maximum-scale=1,user-scalable=no" />
  </head>
  
  <body>
    
    <div id="header">Gate - <%= oMediaClients[sRoom].sName %>:</div>
    
    <div id="opener">
      <video id="gate_cam" autoplay="autoplay">
        <source src="/test.mp4" type="video/mp4" />
      </video>
      <div id="gate_switch" class="button">Open / Close</div>
    </div>
    
  </body>
  
</html>